# raico_base_images
Dockerfiles for RAICo1's base images. Each image is built and published to ghcr.io/adorno-lab only when its own files change, via per-image GitHub Actions workflows triggered on merge to main.
